# 🏗️ هيكل مشروع DARK - دارك

## نظرة عامة
منصة سودانية لتأجير العقارات الموثقة ميدانيًا

## حالة قاعدة البيانات ✅

تم إنشاء قاعدة البيانات بنجاح في `/workspace/database/darq.db`

### الجداول المنشأة (19 جدول):

#### 1. جداول المستخدمين والصلاحيات
- `users` - بيانات المستخدمين الأساسيّة
- `user_profiles` - ملفات تعريف المستخدمين الإضافية
- `subscriptions` - اشتراكات السماسرة

#### 2. جداول العقارات
- `properties` - البيانات الأساسية للعقارات
- `property_types` - أنواع العقارات (شقة، منزل، استوديو...)
- `property_images` - صور العقارات
- `property_amenities` - مرافق العقارات (كهرباء، ماء، إنترنت...)
- `amenities` - قائمة المرافق المتاحة

#### 3. جداول الموقع الجغرافي
- `cities` - المدن السودانية
- `districts` - الأحياء داخل كل مدينة

#### 4. جداول التحقق الميداني
- `field_verifications` - تقارير التحقق الميداني من العقارات

#### 5. جداول المعاملات
- `inquiries` - الاستفسارات والرسائل بين المستخدمين
- `bookings` - حجوزات زيارة العقارات
- `contracts` - عقود الإيجار
- `payments` - المدفوعات والمعاملات المالية

#### 6. جداول النظام
- `audit_log` - سجل تدقيق العمليات (للأمان والشفافية)
- `notifications` - إشعارات المستخدمين
- `settings` - إعدادات النظام العامة

#### 7. العروض (Views)
- `v_verified_properties` - عرض موحّد للعقارات الموثقة النشطة

### البيانات الأولية المُدخلة:

#### المدن (6 مدن):
1. بورتسودان (Port Sudan)
2. مدني (Medani)
3. عطبرة (Atbara)
4. الخرطوم (Khartoum)
5. أم درمان (Omdurman)
6. بحري (Bahri)

#### أنواع العقارات (7 أنواع):
- شقة، منزل، استوديو، غرفة، مكتب، فيلا، أرض

#### المرافق (12 مرفق):
- كهرباء، ماء، إنترنت، تكييف، تدفئة، غسيل، مطبخ، موقف سيارات، حراسة، مصعد، أثاث، شرفة

#### الإعدادات الافتراضية:
- سعر صرف الدولار: 1500 جنيه سوداني
- رسوم التحقق الميداني: 50,000 جنيه
- سعر الإعلان المميز: 10 دولار
- اشتراك الجالية الشهري: 50 دولار

---

## الخطوات التالية المقترحة:

### 1. تطوير واجهة الخلفية (Backend API)
- [ ] إنشاء نقاط API للتسجيل وتسجيل الدخول
- [ ] API لإدارة العقارات (CRUD)
- [ ] API للبحث والفلترة
- [ ] API للتحقق الميداني
- [ ] API للمدفوعات

### 2. تطوير واجهة المستخدم
- [ ] تحديث صفحات HTML الموجودة لربطها بـ API
- [ ] إضافة صفحات تسجيل الدخول والتسجيل
- [ ] صفحة إضافة عقار جديد
- [ ] لوحة تحكم الإدارة

### 3. الميزات الأمنية
- [ ] تشفير كلمات المرور
- [ ] نظام الجلسات (Sessions/JWT)
- [ ] التحقق من الصلاحيات
- [ ] حماية من هجمات XSS و CSRF

### 4. التكاملات الخارجية
- [ ] ربط مع واتساب API للتواصل
- [ ] بوابة دفع (Bankak أو تحويل بنكي)
- [ ] خرائط Google Maps لتحديد المواقع

---

## تشغيل المشروع

### التحقق من قاعدة البيانات:
```bash
python3 backend/init_db.py
```

### الاستعلام المباشر:
```bash
sqlite3 database/darq.db
```

---

## التقنيات المستخدمة:
- **قاعدة البيانات**: SQLite3
- **لغة الواجهة**: Python 3 (مقترح للخلفية)
- **الواجهة الأمامية**: HTML5, CSS3, JavaScript
- **اللغات**: العربية (الأساسية)، الإنجليزية

---

**تاريخ الإنشاء**: 2026-09-18  
**الإصدار**: 1.0.0
